/**
 * The progettofinale.sailingclub package contains all the various packages and classes to make the application work;
 * specifically it contains the SailingClubClientApp class which contains a main to launch the client side application,
 * the SailingClubServer class contains a main to launch the server side application and the ServerThread class which is used by the server to handle all client requests.
 */
package progettofinale.sailingclub;